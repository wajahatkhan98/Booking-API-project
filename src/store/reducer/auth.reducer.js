// import { authConstant } from "../constatnts/index";

const INITIAL_STATE = {
};

export const Auth_Reducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    
    default:
      return state;
  }
};
